# EstimIA — Backend dependencies
# Installer avec : pip install -r requirements.txt

# Data pipeline
pandas==2.2.2
numpy==1.26.4
requests==2.31.0
geopandas==0.14.4       # manipulations spatiales (Phase 1)
shapely==2.0.4          # géométries (requis par geopandas)
pyarrow==16.0.0         # lecture/écriture Parquet rapide
openpyxl==3.1.2

# Machine Learning (Phase 2)
scikit-learn==1.4.2
xgboost==2.0.3
joblib==1.4.2           # sauvegarde modèles .pkl

# API Backend (Phase 3)
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
httpx==0.27.0           # client HTTP async pour FastAPI

# Agent IA (Phase 4) — décommenter quand besoin
# strands-agents==0.1.0
# langchain==0.2.0
# langchain-community==0.2.0

# Interface (Phase 5) — optionnel si Next.js
# streamlit==1.35.0
# streamlit-folium==0.21.0
# folium==0.16.0

# Notebooks & visualisation
jupyter==1.0.0
notebook==7.2.0
matplotlib==3.9.0
seaborn==0.13.2
plotly==5.22.0

# Qualité code
ruff==0.4.7             # linter + formatter (remplace flake8 + black)
pytest==8.2.2